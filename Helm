Kubernetes Helm process for managing different .yaml files. Instead of different files making 

Key components of the kubernates HELM
charts
repositories
Releases
values 

It is used with MySQL for better connection 


Install the VS Code extension Meet Amazon Q for Automate the AWS values 
Among all AI, Amazon is the most relevant AI for DevOps users but the drawback was it required an Amazon ID to Login and a password but the best part was it will create the whole file and everythnik.
